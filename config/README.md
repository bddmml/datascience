# Configuration Directory

This directory contains configuration files for trading strategies and parameters.

## Files

- `strategy_params.yaml` - Strategy parameters and settings
- `data_sources.yaml` - Configuration for data sources and APIs

## Security

**Never commit sensitive information such as:**
- API keys
- API secrets
- Account credentials
- Personal access tokens

Use environment variables or `.env` files (which are gitignored) for sensitive data.
